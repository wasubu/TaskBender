<Window x:Class="TaskBender.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TaskBender"
        mc:Ignorable="d"
        Title="TaskBender Settings" Height="250" Width="400"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Text="Spotlight Key:" VerticalAlignment="Center" Margin="0,0,10,10"/>
        <TextBox x:Name="SpotlightKeyBox" Grid.Column="1" Margin="0,0,0,10" 
                 IsReadOnly="True" PreviewKeyDown="KeyBox_PreviewKeyDown" 
                 GotFocus="KeyBox_GotFocus" LostFocus="KeyBox_LostFocus"
                 Tag="Spotlight"/>

        <TextBlock Grid.Row="1" Text="QuickLook Key:" VerticalAlignment="Center" Margin="0,0,10,10"/>
        <TextBox x:Name="QuickLookKeyBox" Grid.Row="1" Grid.Column="1" Margin="0,0,0,10" 
                 IsReadOnly="True" PreviewKeyDown="KeyBox_PreviewKeyDown"
                 GotFocus="KeyBox_GotFocus" LostFocus="KeyBox_LostFocus"
                 Tag="QuickLook"/>

        <StackPanel Grid.Row="3" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Save" Width="80" Height="30" Click="Save_Click" Margin="0,0,10,0"/>
            <Button Content="Cancel" Width="80" Height="30" Click="Cancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
